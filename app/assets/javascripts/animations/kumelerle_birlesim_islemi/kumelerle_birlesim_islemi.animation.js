var Animation = {
    images:[],
	init:function(container){
		Animation.container = container;
        /*Animation.kumem=new Set({type:Set.SMALLER_THAN,value:5 });
        Animation.kumem.drawVennDiagram(Animation.container,new Point(400,40),"C");*/

        Main.animationFinished(1000);
		}
}